import { SearchItemNamePipe } from './search-item-name.pipe';

describe('SearchItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
